export interface Product {
  id: string
  producerId: string
  name: string
  price: number
  season: string
  quantity: number
  region: string
  description?: string
  photoUrl?: string
  createdAt: string
  updatedAt: string
}
